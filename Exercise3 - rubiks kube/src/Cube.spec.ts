

describe("Cubes", () => {
    it("should rotate on the four on the front when F is chagmed", () => {
        const cube = new Cube();

        expect(cube.getRotation()).toBe({hi: "ho"});
    })
})
